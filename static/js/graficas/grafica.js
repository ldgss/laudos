function grafica() {
    const width = 500;
    const height = 300;

    const svg = d3.select("#grafico-container")
        .append("svg")
        .attr("width", width)
        .attr("height", height);

    // Aquí puedes añadir más código para crear el gráfico
}

grafica();